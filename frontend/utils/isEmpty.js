/**
 * Checks if a string is empty.
 * @param {string} string is the string to be checked.
 * @returns a boolean indicating if the string is empty.
 */
const isEmpty = (string) => string.trim() === "";

export default isEmpty;
